import { Commit } from "./pollFileChanges"
import { Issue } from "./listIssues"

export default function computeDelta(commits: Commit[], issues: Issue[]) {
    console.log(commits)
    console.log(issues)

    return []
}
