{
    "name": "@poll-github-repo/action",
    "version": "0.0.2",
    "scripts": {
        "build": "ncc build src/index.ts -o dist",
        "test": "TEST_ENV=1 jest",
        "build-and-run": "yarn build && node dist/index.js"
    },
    "devDependencies": {
        "@types/jest": "^29.5.5",
        "@vercel/ncc": "^0.38.0",
        "jest": "^29.7.0",
        "ts-jest": "^29.1.1",
        "typescript": "^5.2.2"
    },
    "dependencies": {
        "@actions/core": "^1.10.1",
        "@actions/github": "^5.1.1"
    }
}
